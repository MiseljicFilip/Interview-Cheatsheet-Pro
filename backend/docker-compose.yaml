services:
  postgres:
    image: postgres:16
    container_name: recallstack
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: recallstack
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
